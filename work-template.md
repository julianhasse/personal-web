# Selected Work template

The active copy-ready template now lives at:

```text
content/work/_work-template.md
```

It is marked `draft: true`, so the site loader ignores it until you duplicate and publish it.
